# ============================================================
# Requirements — Proyecto Final PLN (Agentic RAG Mundial 2026)
# ============================================================
# Instalar tras activar el venv:
#   pip install -r requirements.txt
# ------------------------------------------------------------

# === Comunes ===
python-dotenv>=1.0.0
pyyaml>=6.0
tqdm>=4.66.0

# === Fase 3 — Construir corpus externo (notebooks 01-03) ===
wikipedia-api>=0.6.0
pypdf>=4.0.0
pandas>=2.0.0
kagglehub>=0.3.0

# === Fase 5 — Chunking + embeddings + vector DB ===
chromadb>=0.5.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
sentence-transformers>=3.0.0
python-frontmatter>=1.1.0

# === Fase 6 — Agentic RAG con tool calling ===
langchain-openai>=0.2.0
openai>=1.50.0
langgraph>=0.2.0

# === Fase 8 — Evaluacion con RAGAS ===
ragas>=0.2.0
datasets>=3.0.0

# === Jupyter / visualizacion ===
jupyter>=1.1.0
ipykernel>=6.29.0
ipywidgets>=8.1.0
matplotlib>=3.9.0
seaborn>=0.13.0
